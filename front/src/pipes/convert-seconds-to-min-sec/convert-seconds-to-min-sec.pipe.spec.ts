import { ConvertSecondsToMinSecPipe } from './convert-seconds-to-min-sec.pipe';

describe('ConvertSecondsToMinSecPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertSecondsToMinSecPipe();
    expect(pipe).toBeTruthy();
  });
});
